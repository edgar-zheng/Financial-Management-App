spring.config.import=optional:classpath:application-env.properties,optional:file:../.env[.properties],optional:file:./.env[.properties]
spring.application.name=portfolio
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio_tracker
spring.datasource.username=portfolio_app
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Server-side credential; never expose this property in frontend configuration.
market.api.key=${MARKET_API_KEY:}

market.api.base-url=${MASSIVE_BASE_URL:https://api.massive.com}
# End-of-day data: cache successful symbol prices for six hours.
market.cache.ttl=6h
